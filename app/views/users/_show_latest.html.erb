<% group.each_slice(2) do |group| %>
  <div class="row-profile">
    <ul>
      <% group.each do |post| %>
        <li><%= link_to image_tag(post.image.url, class: "img-fluid-profile", alt: "post-image"), post_path(post) %></li>
      <% end %>
    </ul>
  </div>
<% end %>
